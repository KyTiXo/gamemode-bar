{
	"name": "gamemode-bar",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"build": "bun run scripts/build.ts app",
		"build:debug": "bun run scripts/build.ts debug",
		"check": "bun test && bun run lint",
		"commitlint": "commitlint --edit",
		"dev": "bun run build:debug && ./build/debug/GameModeBar",
		"lint": "bunx biome check . && swiftc -typecheck -framework AppKit src/native/GameModeBar.swift",
		"prepare": "husky",
		"readme:assets": "mkdir -p build && swiftc -framework AppKit -O scripts/render-menu-screenshot.swift -o build/render-menu-screenshot && build/render-menu-screenshot .github/assets/menu-screenshot.png",
		"readme:build": "bun run readme:assets && bunx readme-aura build",
		"test": "bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.4",
		"@commitlint/cli": "^21.2.3",
		"@commitlint/config-conventional": "^21.2.3",
		"@types/bun": "latest",
		"husky": "^9.1.7"
	}
}
